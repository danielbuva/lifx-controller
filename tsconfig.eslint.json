{
  "extends": "./tsconfig.json", // Extend your base tsconfig
  "include": [
    "client/**/*", // Include all files in client
    "server/**/*", // Include all files in server
    "/*.config.js", // Include root config files
    "/*.config.ts" // Include root config files
  ]
}
